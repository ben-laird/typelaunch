import test from "ava";

/* I generally keep this sanity test file around just to integration test AVA.
That way I know it's me that messed up tests lol */

test("Passing sanity test", (t) => {
  t.pass("Sanity test passed!");
});
